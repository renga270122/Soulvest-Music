import 'package:flutter/material.dart';

class RitualsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("My Rituals")),
      body: Center(child: Text("Ritual dashboard coming soon...")),
    );
  }
}
